version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
    volumes:
      - ./static/uploads:/app/static/uploads
      - ./blog.db:/app/blog.db
    restart: unless-stopped
